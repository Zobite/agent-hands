services:
  agent-hands:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: agent-hands:latest
    container_name: agent-hands
    restart: unless-stopped
    ports:
      - "18080:18080"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
